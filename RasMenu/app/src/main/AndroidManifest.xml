<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aqsa.rasmenu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/resturant2"
        android:roundIcon="@mipmap/resturant2"
        android:supportsRtl="false"
        android:theme="@style/Theme.RasMenu"
        android:configChanges="orientation"
        android:screenOrientation="portrait">
        <activity android:name=".view_items"/>
        <activity android:name=".sold_item_view" />
        <activity android:name=".Join.join"
            android:label="Join"/>
        <activity android:name=".sign_in"
            android:label="SignIn"/>
        <activity android:name=".sandwichs_category_list"
            android:label="Sandwiches"/>
        <activity android:name=".meals_category_list"
            android:label="Meals"/>
        <activity android:name=".main_categories"
            android:label="Categories">
        </activity>
        <activity android:name=".entrees_category_list"
            android:label="Entrees">
        </activity>
        <activity android:name=".drinks_category_list"
            android:label="Drinks"/>
        <activity android:name=".additions_list"
            android:label="Additions"/>
        <activity android:name=".MainActivity"
            android:label="Majnoun Layla Res.">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>